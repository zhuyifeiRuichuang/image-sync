# 2026-07-09 Work Log

## 容器镜像同步工具完整开发
- 创建了完整的 image-sync 项目
- 文件清单:
  - `.github/workflows/sync-image.yml` - GitHub Actions 工作流
  - `.github/ISSUE_TEMPLATE/sync-image.yml` - Issue 模板
  - `scripts/sync-image.sh` - 镜像同步脚本
  - `.github/registry-config-example` - 配置示例
  - `README.md` - 完整说明文档

## 修复工作流关键缺陷
- 添加 actions/checkout@v4 步骤
- 修复源仓库 embedded 处理
- 添加 REGISTRY_PASSWORD 配置检查

## 修正目标镜像命名规则 (commit 2328970)
- 腾讯云/华为云/阿里云：扁平命名空间，只取镜像名最后一段
  nginxinc/nginx → ruichuangdev/nginx (不是 ruichuangdev/nginxinc/nginx)
- Docker Hub/Quay/GHCR：支持嵌套，保留完整路径
  nginxinc/nginx → username/nginxinc/nginx
- 脚本新增第9参数 REGISTRY_TYPE
- Workflow 传递 vars.REGISTRY_TYPE
- README 更新命名规则

## 延长同步超时时间 (commit 67298d3)
- mysql 大镜像跨国同步超过 30 分钟被取消
- workflow timeout-minutes 从 30 改为 120

## 增加同步进度可见性 (commit 7a64e71)
- 所有日志带时间戳
- skopeo copy 增加 --verbose，显示每层复制详情
- 每30秒打印进度心跳，避免日志长时间无输出
- 同步前显示源镜像层数
- 命令结束后显示完整输出和耗时统计

## 增强错误提示详细信息 (commit 3038a6b)
- 脚本各阶段失败时输出 ERROR_TYPE 和 ERROR_DETAIL：
  - login_failed：捕获 skopeo login 的具体错误输出和退出码
  - source_image_not_found：捕获 skopeo inspect 的具体错误输出和退出码
  - sync_failed：捕获多架构和逐架构同步的退出码及详情
  - partial_sync_failed：区分部分架构失败
- Workflow 失败评论根据 ERROR_TYPE 分类展示：
  - login_failed：展示认证失败详情 + 针对性建议（密码类型说明、本地验证命令）
  - source_image_not_found：展示源镜像检查失败详情 + 建议检查拼写和私有镜像
  - sync_failed：展示同步失败详情 + 建议检查命名空间和权限
- 主流程改为逐阶段 if 检查，失败时立即终止并输出错误类型
- run_with_heartbeat 保存命令输出到全局变量 LAST_CMD_OUTPUT 供诊断
