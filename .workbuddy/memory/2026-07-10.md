## 2026-07-10

- 修复多架构验证日志中出现 `unknown` 的问题：
  - 原因：Docker Buildx 构建的镜像会附带 attestation manifests，platform 为 `unknown/unknown`。
  - 修改 `scripts/sync-image.sh`：在 `inspect_source_image` 和 `verify_sync` 中过滤 `arch == 'unknown' && os == 'unknown'` 的 manifest，并去重。
  - `--all` 同步成功后，使用 `AVAILABLE_ARCHS` 显示源镜像支持的所有真实架构，而不是固定写死 `amd64 arm64`。
  - 增加 `read_result()` 辅助函数从 RESULT_FILE 读取结果。
- 更新 `README.md`：新增 FAQ 解释 `unknown` 架构的原因，并在注意事项中说明。
- 本地已提交 `93af0f1`，因沙箱无 Git credential helper，需手动 push。
